{% extends 'base.html.twig' %}

{% block title %}Hello benjamin!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

	




<table class="table is-hoverable is-narrow">

	<thead>

		<tr>
				<th class="table-head">Index</th>
				<th class="table-head"> année </th>
		    	<th class="table-head"> titre </th>
		    	<th class="table-head">ID Imdb</th>
		      	<th class="table-head"> Poster </th>
		      	
		</tr>
		    

	</thead>

	<tbody>

	{% for key,movie in movies %}
		
		  
		  	<tr>
		  		<td class="">{{loop.index}}</td>
		  		<td class="movie--year">{{ movie.Year }}</td>
		  		<td class="movie--title">{{ movie.Title }}</td>
		  		<td class="movie--id">{{movie.imdbID}}</td>
		  		<td class=""> 
		  			<a href=" {{ movie.Poster }}" target="_blank">
		  				
		  				<img src=" {{ movie.Poster }}" style="max-width:60%;height:auto;" class="movie--img">

		  			</a>
		  			
		  		</td>
		  		

		  	</tr>
		  	

	{%endfor%}

	</tbody>
</table>

{# 
    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'src/Controller/OmdbController.php'|file_link(0) }}">src/Controller/OmdbController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'templates/omdb/index.html.twig'|file_link(0) }}">templates/omdb/index.html.twig</a></code></li>
    </ul> #}


</div>
{% endblock %}
